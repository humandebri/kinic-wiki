[package]
name = "wiki-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
wiki-store = { path = "../wiki_store" }
wiki-types = { path = "../wiki_types" }

[dev-dependencies]
rusqlite = { version = "0.37.0", features = ["bundled"] }
tempfile = "3.23.0"
